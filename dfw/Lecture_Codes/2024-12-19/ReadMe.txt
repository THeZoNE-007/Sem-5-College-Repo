discussed two website for pcap file analysis: 

site1 : lab.dynamite.ai
site2: apackets.com

Discussed scapy module in python and used code progs on pcap files with scapy imported in it

Note: You need to create virtual environment ( How to? on -> Lecture 14 pg 8 on site ) to install scapy using "pip install scapy" cmd
